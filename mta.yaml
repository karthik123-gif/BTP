_schema-version: "3.2"
ID: library
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: library-destination-content
  type: com.sap.application.content
  requires:
  - name: library-destination-service
    parameters:
      content-target: true
  - name: library-repo-host
    parameters:
      service-key:
        name: library-repo-host-key
  - name: library-uaa
    parameters:
      service-key:
        name: library-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: library_html_repo_host
          ServiceInstanceName: library-html5-srv
          ServiceKeyName: library-repo-host-key
          sap.cloud.service: library
        - Authentication: OAuth2UserTokenExchange
          Name: library_uaa
          ServiceInstanceName: library-xsuaa-srv
          ServiceKeyName: library-uaa-key
          sap.cloud.service: library
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: library-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: library-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - library.zip
      name: library
      target-path: resources/
- name: library
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: library-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: library-destination-service
    service-plan: lite
- name: library-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: library-xsuaa-srv
    service-plan: application
- name: library-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: library-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
